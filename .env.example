# Self-hosted Supabase Remote MCP Server Configuration

# Required: Supabase instance URL
SUPABASE_URL=http://localhost:8000

# Required: Supabase anonymous key
SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Supabase service role key (for admin operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: Direct database connection URL (for PostgreSQL fallback)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Optional: Supabase JWT secret (for auth operations)
SUPABASE_AUTH_JWT_SECRET=your-jwt-secret-here

# Server configuration
PORT=3000
NODE_ENV=production

# Optional: Workspace path for file operations
WORKSPACE_PATH=/app/workspace
